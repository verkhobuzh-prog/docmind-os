# Doc-Hub — повна конфігурація з пінованою FalkorDB
# ADR-001: FalkorDB обрано над Neo4j
# docs/adr/ADR-001-graph-database-engine.md
#
# Примітки адаптації під repo:
# - migrations: ./infra/supabase/migrations (не ./backend/db/migrations)
# - FalkorDB v4.18.6 (ADR-001; не v4.0.5 / latest)
# - Backend читає GRAPH_DB_* (compose будує URL з FALKORDB_PASSWORD)

version: "3.9"

networks:
  dochub_net:
    driver: bridge
    name: dochub_net

volumes:
  postgres_data:
    name: dochub_postgres_data
  redis_data:
    name: dochub_redis_data
  falkordb_data:
    name: dochub_falkordb_data
  backend_cache:
    name: dochub_backend_cache

services:
  # ── POSTGRESQL + pgvector (vector RAG) ───────────────────────────────────
  postgres:
    image: pgvector/pgvector:pg16
    container_name: dochub-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-dochub}
      POSTGRES_USER: ${POSTGRES_USER:-dochub}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-dochub}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/supabase/migrations:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-dochub} -d ${POSTGRES_DB:-dochub}",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - dochub_net

  # ── REDIS (cache + rate limiting) ────────────────────────────────────────
  redis:
    image: redis:7.2-alpine
    container_name: dochub-redis
    restart: unless-stopped
    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD:-dochub_redis_secret}
      --appendonly yes
      --appendfilename redis.aof
      --maxmemory ${REDIS_MAX_MEMORY:-256mb}
      --maxmemory-policy allkeys-lru
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test:
        [
          "CMD",
          "redis-cli",
          "-a",
          "${REDIS_PASSWORD:-dochub_redis_secret}",
          "ping",
        ]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s
    networks:
      - dochub_net

  # ── FALKORDB (Knowledge Graph — Legal Domain) ────────────────────────────
  falkordb:
    image: falkordb/falkordb:v4.18.6
    container_name: dochub-falkordb
    restart: unless-stopped
    environment:
      REDIS_ARGS: >-
        --requirepass ${FALKORDB_PASSWORD:-dochub_graph_secret}
        --appendonly yes
        --appendfilename falkordb.aof
      FALKORDB_MAX_QUEUED_QUERIES: ${FALKORDB_MAX_QUEUED_QUERIES:-25}
      REDIS_SAVE: "60 1 300 100"
    ports:
      - "${FALKORDB_PORT:-6380}:6379"
    volumes:
      - falkordb_data:/data
    healthcheck:
      test:
        - CMD-SHELL
        - >
          redis-cli -p 6379
          -a "${FALKORDB_PASSWORD:-dochub_graph_secret}"
          --no-auth-warning
          GRAPH.QUERY health_check "RETURN 1"
          | grep -q "1" || exit 1
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - dochub_net

  # ── BACKEND (FastAPI) ────────────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: development
    container_name: dochub-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      API_HOST: 0.0.0.0
      API_PORT: 8000
      DATABASE_URL: postgresql://${POSTGRES_USER:-dochub}:${POSTGRES_PASSWORD:-dochub}@postgres:5432/${POSTGRES_DB:-dochub}
      REDIS_URL: redis://:${REDIS_PASSWORD:-dochub_redis_secret}@redis:6379/0
      GRAPH_DB_ENABLED: ${GRAPH_DB_ENABLED:-true}
      GRAPH_DB_URL: redis://:${FALKORDB_PASSWORD:-dochub_graph_secret}@falkordb:6379
      GRAPH_DB_NAME: ${GRAPH_DB_NAME:-dochub_knowledge}
      ENVIRONMENT: ${ENVIRONMENT:-development}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      LOG_JSON: ${LOG_JSON:-false}
    ports:
      - "${BACKEND_PORT:-${API_PORT:-8000}}:8000"
    volumes:
      - ./backend:/app:delegated
      - backend_cache:/root/.cache
      - /app/__pycache__
      - /app/.pytest_cache
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      falkordb:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - dochub_net

  # ── FRONTEND (React + Vite) ──────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: development
    container_name: dochub-frontend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      VITE_SUPABASE_URL: ${SUPABASE_URL}
      VITE_SUPABASE_ANON_KEY: ${SUPABASE_ANON_KEY}
      VITE_API_URL: http://localhost:${BACKEND_PORT:-${API_PORT:-8000}}
    ports:
      - "${FRONTEND_PORT:-5173}:5173"
    volumes:
      - ./frontend:/app:delegated
      - /app/node_modules
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - dochub_net
